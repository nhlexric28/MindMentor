services:
  - name: mindmentor-backend
    type: web
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 4 -b 0.0.0.0:5000 ai_server:app
    plan: free
    envVars:
      - key: PORT
        value: 5000